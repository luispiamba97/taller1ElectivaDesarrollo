document.addEventListener('DOMContentLoaded', () => {
    const boton = document.getElementById('btnHola');

    boton.addEventListener('click', () => {
        console.log("Hola Mundo desde JavaScript");
    });
});